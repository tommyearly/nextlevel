# Postgres (Vercel Postgres, Neon, or Supabase). See docs/VERCEL_DATABASE.md
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
RESEND_API_KEY="re_xxxx"
# Min 32 chars. Avoid # (can be treated as comment and truncate the value)
SESSION_SECRET="your-secret-min-32-chars"
# Production: set to your apex domain with leading dot so www and non-www share the session cookie (e.g. .nextlevelweb.ie)
# COOKIE_DOMAIN=".nextlevelweb.ie"
ADMIN_EMAIL="admin@nextlevelweb.ie"

# Stripe — add price IDs for each tier you want to offer online payment (deposit + balance)
# STRIPE_SECRET_KEY="sk_live_..."
# STRIPE_PRICE_STARTER_DEPOSIT="price_..."   # €200 deposit
# STRIPE_PRICE_STARTER_BALANCE="price_..."   # balance (€700)
# STRIPE_PRICE_GROWTH_DEPOSIT="price_..."    # optional: Growth deposit
# STRIPE_PRICE_GROWTH_BALANCE="price_..."    # optional: Growth balance (for upgrades)
# STRIPE_PRICE_PREMIUM_DEPOSIT="price_..."   # optional: Premium deposit
# STRIPE_PRICE_PREMIUM_BALANCE="price_..."  # optional: Premium balance (for upgrades)
# STRIPE_WEBHOOK_SECRET="whsec_..."          # for checkout.session.completed + receipt emails

# reCAPTCHA v2 (contact form). Get keys at https://www.google.com/recaptcha/admin
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY="6Lc..."    # public, used in browser
# RECAPTCHA_SECRET_KEY="6Lc..."              # server-only, used to verify token
